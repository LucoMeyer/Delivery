import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '354 INF Delivery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
